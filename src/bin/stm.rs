//! stm — ScrollingTilingManager CLI client
//!
//! Sends commands to the stmd daemon, queries state, and edits config.

fn main() {
    // TODO: Parse CLI args via clap, connect to daemon IPC, forward commands
    println!("stm: CLI not yet implemented");
}
